// packages/verify/index.ts — verification matrix adapters.
// lint / typecheck / build / unit / integration / e2e / secretScan /
// depVuln / migrationSmoke / playwrightRuntime / screenshotDiff.
// Real adapters land in Step 10.
export const placeholder = true;
